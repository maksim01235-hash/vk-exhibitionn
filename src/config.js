/**
 * Конфигурация приложения «Выставка».
 * 
 * Все настраиваемые параметры собраны здесь.
 * При деплое на новый проект достаточно изменить SHEET_URL.
 * 
 * Как опубликовать Google Таблицу:
 *   1. Откройте таблицу → Файл → Поделиться → Опубликовать в интернете
 *   2. Вкладка «Веб-страница» → формат «Значения, разделённые запятыми (.csv)»
 *   3. Нажмите «Опубликовать»
 *   4. Скопируйте ссылку и вставьте в SHEET_URL ниже
 * 
 * Структура таблицы (заголовки столбцов):
 *   id, order, title, photographer, description, funFact,
 *   imageUrl, imagePreviewUrl, originalUrl, category,
 *   camera, lens, iso, aperture, shutterSpeed, focalLength, ...
 *   (можно добавлять любые столбцы — они появятся в техпараметрах)
 */

// ═══════════════════════════════════════
// ОСНОВНЫЕ НАСТРОЙКИ
// ═══════════════════════════════════════

const CONFIG = {
  /**
   * Ссылка на опубликованную Google Таблицу в формате CSV.
   * 
   * Как получить:
   *   1. Google Таблица → Файл → Опубликовать в интернете
   *   2. Выбрать формат «Значения, разделённые запятыми (.csv)»
   *   3. Скопировать ссылку (должна заканчиваться на ?output=csv)
   * 
   * Для смены источника данных — просто замените эту ссылку.
   */
  SHEET_URL: 'https://docs.google.com/spreadsheets/d/e/2PACX-1vQj-JFU4pGi_7Q-dPvxXsFPCGMeAlg0fccBBNsnlXymBEn3AmEg7h0HbkV85Vi62a8BYOZav93Iv_50/pub?output=csv',

  /**
   * Время жизни кеша данных в минутах.
   * Сейчас НЕ ИСПОЛЬЗУЕТСЯ — данные загружаются при каждом открытии.
   * Оставлено для будущей реализации фонового обновления.
   */
  CACHE_TTL: 30,

  /**
   * Количество фотографий на странице в галерее.
   * Сейчас НЕ ИСПОЛЬЗУЕТСЯ — все фото отображаются сразу.
   * Оставлено для будущей реализации пагинации/виртуального скролла.
   */
  PHOTOS_PER_PAGE: 50,
};

export default CONFIG;